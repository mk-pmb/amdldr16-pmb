/*jslint indent: 2, maxlen: 80, node: true */
/* -*- tab-width: 2 -*- */
'use strict';

var bug = { url: 'https://github.com/cujojs/curl/issues/300' };
module.exports = bug;
if (bug) { throw new Error("Unfortunately, you won't see this"); }
bug.stillAlive = 'impossibru';
